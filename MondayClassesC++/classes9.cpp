#include <iostream>
#include <vector>
#include <string>
#include <sstream>

using  namespace std;

int main() {

    std::vector<std::string> places = {};
    std::vector<std::string> activities = {};
    
    std::string input;
    while (getline(cin, input) && input != "-1")
    {
        istringstream inputs(input);
        int countPlace = 0;
        int countActiviti = 0;
        if (input != "-1")//place/activity check instead?
        {
            
        }
        
        
    }
    

    
    return 0;
}